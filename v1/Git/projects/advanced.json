[
    {
        "title": "Reverting Commits ↩️",
        "ques": "You made a mistake in the last commit `abc1234` and pushed it. You need to undo the changes by creating a **new commit** that reverses the effect of the bad commit.\n\nRevert the commit `abc1234`.",
        "answer": {
            "type": "code",
            "lang": "bash",
            "content": "git revert abc1234"
        },
        "explanation": "**`git revert`** creates a *new* commit that is the exact opposite of the specified commit, making it safe for shared history."
    },
    {
        "title": "Soft Reset 🍦",
        "ques": "You want to undo your last commit but **keep** your changes in the staging area to fix them.\n\nPerform a **soft reset** to the previous commit (`HEAD~1`).",
        "answer": {
            "type": "code",
            "lang": "bash",
            "content": "git reset --soft HEAD~1"
        },
        "explanation": "**`git reset --soft`** moves the HEAD pointer back but leaves your index (staging area) and working directory unchanged."
    },
    {
        "title": "Hard Reset 🔨",
        "ques": "You want to completely discard all local changes and commits, resetting your branch to match `origin/main` exactly.\n\nPerform a **hard reset**.",
        "answer": {
            "type": "code",
            "lang": "bash",
            "content": "git reset --hard origin/main"
        },
        "explanation": "**`git reset --hard`** resets the index and working tree. Any changes to tracked files in the working tree since `<commit>` are discarded."
    },
    {
        "title": "Adding Submodules 🧩",
        "ques": "You want to include another Git repository as a subdirectory in your project.\n\nAdd the repository `https://github.com/lib/lib.git` as a submodule in the folder `libs/my-lib`.",
        "answer": {
            "type": "code",
            "lang": "bash",
            "content": "git submodule add https://github.com/lib/lib.git libs/my-lib"
        },
        "explanation": "**`git submodule add`** clones an external repository into a specific path and adds it to the `.gitmodules` file."
    },
    {
        "title": "Git Flow Initialization 🌊",
        "ques": "Which command initializes a repository to use the **Gitflow** branching model with default branch names?",
        "answer": {
            "type": "code",
            "lang": "bash",
            "content": "git flow init"
        },
        "explanation": "**`git flow init`** sets up the configuration for the git-flow branching model, asking for branch naming conventions."
    }
]