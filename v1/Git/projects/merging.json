[
    {
        "title": "Merging a Branch 🔗",
        "ques": "You are on the **`main`** branch and want to incorporate changes from **`feature-login`**.\n\nRun the command to **merge** `feature-login` into your current branch.",
        "answer": {
            "type": "code",
            "lang": "bash",
            "content": "git merge feature-login"
        },
        "explanation": "**`git merge <branch>`** combines the history of the specified branch into the current branch."
    },
    {
        "title": "Rebasing a Branch 🏗️",
        "ques": "You are working on **`feature-login`** and want to update it with the latest changes from **`main`** by moving your commits to the tip of `main`.\n\nRun the command to **rebase** your current branch onto `main`.",
        "answer": {
            "type": "code",
            "lang": "bash",
            "content": "git rebase main"
        },
        "explanation": "**`git rebase main`** rewrites your project history by creating new commits for each commit in your original branch, placing them after the last commit of `main`."
    },
    {
        "title": "Aborting a Conflict 🛑",
        "ques": "You started a merge, but ran into too many conflicts and want to back out.\n\nWhat command stops the merge and returns the repository to its state before the merge started?",
        "answer": {
            "type": "code",
            "lang": "bash",
            "content": "git merge --abort"
        },
        "explanation": "**`git merge --abort`** stops the merge process and resets the state of the repository to how it was before the merge command was run."
    },
    {
        "title": "Interactive Rebase (Squashing) 🥞",
        "ques": "You have 5 small commits on your branch and want to combine them into a cleaner history before merging.\n\nStart an **interactive rebase** for the last 5 commits.",
        "answer": {
            "type": "code",
            "lang": "bash",
            "content": "git rebase -i HEAD~5"
        },
        "explanation": "**`git rebase -i`** opens an interactive editor where you can squash, fixup, reword, or drop commits to clean up your history."
    },
    {
        "title": "Rebase vs. Merge 🤔",
        "ques": "You want to integrate changes from `main` into your feature branch **without** creating an extra merge commit, keeping a **linear history**.\n\nWhich command should you use: `git merge main` or `git rebase main`?",
        "answer": {
            "type": "text",
            "content": "**`git rebase main`**"
        },
        "explanation": "**Rebasing** creates a linear history by moving your commits on top of the base branch, whereas **merging** preserves the history structure but creates a merge commit."
    }
]