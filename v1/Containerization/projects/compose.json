[
    {
        "title": "Starting Services 🚀",
        "ques": "You have a `docker-compose.yml` file. You want to start all services defined in it in the background.\n\nWrite the command.",
        "answer": {
            "type": "code",
            "lang": "bash",
            "content": "docker-compose up -d"
        },
        "explanation": "**`docker-compose up -d`** builds, (re)creates, starts, and attaches to containers for a service in detached mode."
    },
    {
        "title": "Compose File Structure 🏗️",
        "ques": "Under which top-level key do you define the containers (like `web`, `db`) in `docker-compose.yml`?",
        "answer": {
            "type": "text",
            "content": "**`services:`**"
        },
        "explanation": "The **`services`** key contains the configuration for each container in your application."
    },
    {
        "title": "Stopping Services 🛑",
        "ques": "You want to stop and remove all containers, networks, and volumes defined in your Compose file.\n\nWrite the command.",
        "answer": {
            "type": "code",
            "lang": "bash",
            "content": "docker-compose down"
        },
        "explanation": "**`docker-compose down`** stops running containers and removes them along with networks and volumes created by `up`."
    },
    {
        "title": "Dependencies 🔗",
        "ques": "Your `web` service needs the `database` service to be started first. \n\nWhich option in `docker-compose.yml` defines this start order?",
        "answer": {
            "type": "code",
            "lang": "yaml",
            "content": "depends_on:\n  - database"
        },
        "explanation": "**`depends_on`** expresses dependency between services, causing `docker-compose up` to start services in dependency order."
    },
    {
        "title": "Scaling ⚖️",
        "ques": "You want to run 3 instances of the `worker` service defined in your Compose file.\n\nWrite the command to bring up the environment with this scale.",
        "answer": {
            "type": "code",
            "lang": "bash",
            "content": "docker-compose up --scale worker=3"
        },
        "explanation": "The **`--scale`** flag allows you to specify the number of containers to start for a service."
    }
]