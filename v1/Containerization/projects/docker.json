[
    {
        "title": "Running a Container 🏃",
        "ques": "You want to start a new container from the `nginx` image and detach it (run in background).\n\nWrite the command to do this.",
        "answer": {
            "type": "code",
            "lang": "bash",
            "content": "docker run -d nginx"
        },
        "explanation": "**`docker run -d`** starts a container in detached mode, meaning it runs in the background."
    },
    {
        "title": "Building an Image 🏗️",
        "ques": "You have a `Dockerfile` in the current directory. You want to build an image named `my-app` from it.\n\nWrite the build command.",
        "answer": {
            "type": "code",
            "lang": "bash",
            "content": "docker build -t my-app ."
        },
        "explanation": "**`docker build -t <name> .`** builds an image from the Dockerfile in the current directory (`.`) and tags it with a name."
    },
    {
        "title": "Dockerfile Basics 📄",
        "ques": "Which instruction must be the first non-comment line in a Dockerfile to specify the base image?",
        "answer": {
            "type": "code",
            "lang": "dockerfile",
            "content": "FROM ubuntu:latest"
        },
        "explanation": "**`FROM`** specifies the parent image from which you are building your new image."
    },
    {
        "title": "Executing Commands 💻",
        "ques": "You have a running container with ID `abc1234`. You want to open an interactive bash shell inside it.\n\nWrite the command.",
        "answer": {
            "type": "code",
            "lang": "bash",
            "content": "docker exec -it abc1234 bash"
        },
        "explanation": "**`docker exec -it <id> bash`** opens an interactive terminal session inside the running container."
    },
    {
        "title": "Listing Containers 📋",
        "ques": "You want to see a list of **all** containers, including those that are stopped.\n\nWrite the command.",
        "answer": {
            "type": "code",
            "lang": "bash",
            "content": "docker ps -a"
        },
        "explanation": "**`docker ps -a`** lists all containers, filtering out none (shows stopped, exited, and running containers)."
    }
]